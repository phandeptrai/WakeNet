# Copy file này thành .env và chỉnh theo máy bạn.

# SQLite database file (có thể để path tương đối theo thư mục root)
WAKENET_SQLITE_PATH=./WakeNetData/wakenet.db

# CORS origins cho frontend (phân tách bằng , hoặc ;)
WAKENET_CORS_ORIGINS=http://localhost:3000,https://localhost:3000

